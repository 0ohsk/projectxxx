spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/projextx_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: spring
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: false


logging.level:
  org.hibernate.SQL: debug
#    org.hibernate.type: trace